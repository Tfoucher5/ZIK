# Zikle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build "Zikle", a standalone daily song-guessing mini-game (extrait audio qui s'allonge, 6 essais, partage type Wordle), playable without account, with account-based streak/leaderboard/XP and login-gated replay of past days.

**Architecture:** New Supabase tables (`zikle_pool`, `daily_songs`, `daily_results`) + SQL RPCs for atomic daily pick and result persistence. A server service (`src/lib/server/services/zikle.js`) owns pool refresh (Deezer chart import) and daily-song selection. Three thin API endpoints (`/api/tracks/search`, `/api/zikle/guess`, `/api/zikle/complete`) back the client game. New SvelteKit routes under `/zikle` reuse the existing `(site)` layout, auth context (`getContext('zik')`), and Supabase client patterns already used by `profile`/`playlists`.

**Tech Stack:** SvelteKit 5 (runes), Supabase (Postgres + supabase-js), vitest, existing Deezer public API integration pattern (`src/lib/server/services/deezer.js`, `fetch.js`).

## Global Constraints

- Répondre/commenter en français, pas de commentaires inutiles (spec ZIK).
- Pas de compte requis pour jouer le jour courant ; compte obligatoire pour rejouer un jour passé (spec, section "Décisions validées").
- Jamais le même titre avant 365 jours depuis sa dernière apparition (spec, "Décisions validées").
- Pool de titres = charts Deezer uniquement, jamais le catalogue `tracks` existant (spec, "Décisions validées").
- Le client ne doit jamais recevoir `artist`/`title`/`cover_url` du titre du jour avant la fin de la manche (spec, "Sélection du jour").
- La comparaison de réponse ne se fait jamais côté client (spec, "Vérification de réponse").
- Écritures sur `zikle_pool`/`daily_songs`/`daily_results` uniquement via service role (RPC + admin client), jamais via policy INSERT côté client (spec + convention existante `achievements`/`user_achievements`).
- Tester en local sur la branche `feat/zikle` avant tout déploiement (déjà créée, spec commitée dessus).

---

## Task 1: Migration SQL (tables + RPCs)

**Files:**

- Create: `supabase/migrations/20260804_zikle.sql`

**Interfaces:**

- Produces (used by all later tasks): tables `zikle_pool(track_id uuid PK, added_at timestamptz)`, `daily_songs(id uuid PK, date date UNIQUE, track_id uuid, created_at timestamptz)`, `daily_results(id uuid PK, user_id uuid, date date, attempts int, won bool, solve_time_seconds int, guesses jsonb, created_at timestamptz, UNIQUE(user_id,date))`.
- Produces RPCs: `pick_daily_song()` → `TABLE(date date, track_id uuid, is_new boolean)`, `zikle_complete(p_user_id uuid, p_date date, p_attempts int, p_won boolean, p_solve_time_seconds int, p_guesses jsonb)` → `TABLE(attempts int, won boolean, solve_time_seconds int, is_new boolean)`, `zikle_leaderboard(p_date date)` → `TABLE(username text, attempts int, solve_time_seconds int)`.

- [ ] **Step 1: Write the migration file**

```sql
-- supabase/migrations/20260804_zikle.sql
-- Zikle : mini-jeu "chanson du jour" (extrait audio, 6 essais, streak/leaderboard).
-- Additive, ne touche aucune table existante.

-- 1. Pool de titres candidats (charts Deezer importés au fil du temps)
create table if not exists public.zikle_pool (
  track_id uuid primary key references public.tracks(id) on delete cascade,
  added_at timestamptz not null default now()
);
alter table public.zikle_pool enable row level security;
-- Pas de policy : lecture/écriture uniquement via service role (refresh du pool).

-- 2. Titre tiré au sort pour chaque jour
create table if not exists public.daily_songs (
  id uuid primary key default gen_random_uuid(),
  date date not null unique,
  track_id uuid not null references public.tracks(id),
  created_at timestamptz not null default now()
);
alter table public.daily_songs enable row level security;
-- Pas de policy : lecture uniquement via service role (ne jamais exposer artist/title
-- au client avant la fin de la manche, cf. endpoints /api/zikle/*).

-- 3. Résultats des joueurs connectés
create table if not exists public.daily_results (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.profiles(id) on delete cascade,
  date date not null,
  attempts int not null check (attempts between 1 and 6),
  won boolean not null,
  solve_time_seconds int,
  guesses jsonb not null default '[]',
  created_at timestamptz not null default now(),
  unique (user_id, date)
);
create index if not exists idx_daily_results_date on public.daily_results(date);
alter table public.daily_results enable row level security;
drop policy if exists daily_results_select_own on public.daily_results;
create policy daily_results_select_own on public.daily_results
  for select using (user_id = auth.uid());
-- Pas de policy INSERT/UPDATE : écritures uniquement via la RPC zikle_complete (service role),
-- pour empêcher un client de s'auto-déclarer gagnant sans passer par la vérification serveur.

-- 4. Pick atomique du titre du jour (Europe/Paris), exclut les 365 derniers jours
create or replace function public.pick_daily_song()
returns table(date date, track_id uuid, is_new boolean)
language plpgsql security definer set search_path = public as $$
declare
  today date := (now() at time zone 'Europe/Paris')::date;
  existing uuid;
  picked uuid;
begin
  select ds.track_id into existing from public.daily_songs ds where ds.date = today;
  if existing is not null then
    return query select today, existing, false;
    return;
  end if;

  select zp.track_id into picked
  from public.zikle_pool zp
  where zp.track_id not in (
    select ds.track_id from public.daily_songs ds where ds.date > today - interval '365 days'
  )
  order by random()
  limit 1;

  if picked is null then
    return; -- pool épuisé ou pas encore alimenté : aucune ligne renvoyée
  end if;

  insert into public.daily_songs (date, track_id) values (today, picked)
    on conflict (date) do nothing;

  select ds.track_id into existing from public.daily_songs ds where ds.date = today;
  return query select today, existing, true;
end;
$$;
revoke execute on function public.pick_daily_song() from anon, authenticated;

-- 5. Enregistre un résultat (idempotent) + bonus XP si victoire
create or replace function public.zikle_complete(
  p_user_id uuid,
  p_date date,
  p_attempts int,
  p_won boolean,
  p_solve_time_seconds int,
  p_guesses jsonb
) returns table(attempts int, won boolean, solve_time_seconds int, is_new boolean)
language plpgsql security definer set search_path = public as $$
declare
  existing record;
  inserted record;
begin
  select dr.attempts, dr.won, dr.solve_time_seconds into existing
  from public.daily_results dr where dr.user_id = p_user_id and dr.date = p_date;

  if found then
    return query select existing.attempts, existing.won, existing.solve_time_seconds, false;
    return;
  end if;

  insert into public.daily_results (user_id, date, attempts, won, solve_time_seconds, guesses)
  values (p_user_id, p_date, p_attempts, p_won, p_solve_time_seconds, p_guesses)
  on conflict (user_id, date) do nothing
  returning attempts, won, solve_time_seconds into inserted;

  if inserted is null then
    select dr.attempts, dr.won, dr.solve_time_seconds into existing
    from public.daily_results dr where dr.user_id = p_user_id and dr.date = p_date;
    return query select existing.attempts, existing.won, existing.solve_time_seconds, false;
    return;
  end if;

  if p_won then
    update public.profiles set xp = xp + 10 where id = p_user_id;
  end if;

  return query select inserted.attempts, inserted.won, inserted.solve_time_seconds, true;
end;
$$;
revoke execute on function public.zikle_complete(uuid,date,int,boolean,int,jsonb) from anon, authenticated;

-- 6. Classement du jour (gagnants uniquement, top 20)
create or replace function public.zikle_leaderboard(p_date date)
returns table(username text, attempts int, solve_time_seconds int)
language sql security definer set search_path = public as $$
  select p.username, dr.attempts, dr.solve_time_seconds
  from public.daily_results dr
  join public.profiles p on p.id = dr.user_id
  where dr.date = p_date and dr.won = true
  order by dr.attempts asc, dr.solve_time_seconds asc nulls last
  limit 20;
$$;
revoke execute on function public.zikle_leaderboard(date) from anon, authenticated;
```

- [ ] **Step 2: Appliquer la migration via le MCP Supabase**

Utiliser l'outil `mcp__plugin_supabase_supabase__apply_migration` avec `name: "20260804_zikle"` et le contenu SQL ci-dessus.

- [ ] **Step 3: Vérifier que tout est en place**

Utiliser `mcp__plugin_supabase_supabase__execute_sql` avec :

```sql
select table_name from information_schema.tables
where table_schema = 'public' and table_name in ('zikle_pool','daily_songs','daily_results');

select proname from pg_proc where proname in ('pick_daily_song','zikle_complete','zikle_leaderboard');
```

Expected: les 3 tables et les 3 fonctions apparaissent.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260804_zikle.sql
git commit -m "feat(zikle): migration tables + RPCs (pool, daily_songs, daily_results)"
```

---

## Task 2: Module pur partagé (`src/lib/zikle/shared.js`)

**Files:**

- Create: `src/lib/zikle/shared.js`
- Test: `src/lib/zikle/__tests__/shared.test.js`

**Interfaces:**

- Consumes: rien (module pur, aucune dépendance serveur/DB).
- Produces (utilisé par les tasks 4, 6, 7, 8, 9, 10) : `SNIPPET_DURATIONS` (array de 6 int), `MAX_ATTEMPTS` (=6), `durationForAttempt(attemptIndex: number): number`, `todayParis(date?: Date): string` (YYYY-MM-DD), `computeStreak(results: {date:string, won:boolean}[], today?: string): number`, `buildShareGrid(dayNumber: number, guesses: string[], correctTrackId: string, won: boolean): string`, `escapeIlike(q: string): string`, `dedupById(rows: {id:string}[]): {id:string}[]`.

- [ ] **Step 1: Write the failing tests**

```javascript
// src/lib/zikle/__tests__/shared.test.js
import { describe, it, expect } from "vitest";
import {
  SNIPPET_DURATIONS,
  durationForAttempt,
  todayParis,
  computeStreak,
  buildShareGrid,
  escapeIlike,
  dedupById,
} from "../shared.js";

describe("durationForAttempt", () => {
  it("suit les paliers 1,2,4,7,11,16", () => {
    expect(SNIPPET_DURATIONS).toEqual([1, 2, 4, 7, 11, 16]);
    expect(durationForAttempt(0)).toBe(1);
    expect(durationForAttempt(5)).toBe(16);
  });
  it("clamp au dernier palier si index hors bornes", () => {
    expect(durationForAttempt(10)).toBe(16);
  });
});

describe("todayParis", () => {
  it("formate en YYYY-MM-DD", () => {
    const d = new Date("2026-08-04T22:30:00Z");
    expect(todayParis(d)).toBe("2026-08-05"); // 00h30 à Paris (UTC+2 en août)
  });
});

describe("computeStreak", () => {
  it("0 si jamais joué", () => {
    expect(computeStreak([], "2026-08-04")).toBe(0);
  });
  it("compte les jours consécutifs gagnés en remontant depuis aujourd'hui", () => {
    const results = [
      { date: "2026-08-04", won: true },
      { date: "2026-08-03", won: true },
      { date: "2026-08-02", won: true },
      { date: "2026-08-01", won: false },
    ];
    expect(computeStreak(results, "2026-08-04")).toBe(3);
  });
  it("si aujourd'hui pas encore joué, part d'hier", () => {
    const results = [
      { date: "2026-08-03", won: true },
      { date: "2026-08-02", won: true },
    ];
    expect(computeStreak(results, "2026-08-04")).toBe(2);
  });
  it("un trou casse la série", () => {
    const results = [
      { date: "2026-08-04", won: true },
      { date: "2026-08-02", won: true },
    ];
    expect(computeStreak(results, "2026-08-04")).toBe(1);
  });
});

describe("buildShareGrid", () => {
  it("victoire au 3e essai", () => {
    const txt = buildShareGrid(47, ["a", "b", "c"], "c", true);
    expect(txt).toContain("Zikle #47");
    expect(txt).toContain("3/6");
    expect(txt).toContain("🟥🟥🟩");
    expect(txt).not.toContain("c"); // pas de fuite du track_id/titre
  });
  it("défaite", () => {
    const txt = buildShareGrid(47, ["a", "b", "c", "d", "e", "f"], "z", false);
    expect(txt).toContain("X/6");
    expect(txt).toContain("🟥🟥🟥🟥🟥🟥");
  });
});

describe("escapeIlike", () => {
  it("échappe % et _", () => {
    expect(escapeIlike("50% love_song")).toBe("50\\% love\\_song");
  });
});

describe("dedupById", () => {
  it("garde la première occurrence de chaque id", () => {
    const rows = [
      { id: "1", n: "a" },
      { id: "2", n: "b" },
      { id: "1", n: "c" },
    ];
    expect(dedupById(rows)).toEqual([
      { id: "1", n: "a" },
      { id: "2", n: "b" },
    ]);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/lib/zikle/__tests__/shared.test.js`
Expected: FAIL (module `../shared.js` introuvable).

- [ ] **Step 3: Write the implementation**

```javascript
// src/lib/zikle/shared.js
export const SNIPPET_DURATIONS = [1, 2, 4, 7, 11, 16];
export const MAX_ATTEMPTS = SNIPPET_DURATIONS.length;

export function durationForAttempt(attemptIndex) {
  const i = Math.min(Math.max(attemptIndex, 0), SNIPPET_DURATIONS.length - 1);
  return SNIPPET_DURATIONS[i];
}

export function todayParis(date = new Date()) {
  return new Intl.DateTimeFormat("en-CA", { timeZone: "Europe/Paris" }).format(
    date,
  );
}

function addDaysStr(dateStr, delta) {
  const d = new Date(`${dateStr}T00:00:00Z`);
  d.setUTCDate(d.getUTCDate() + delta);
  return d.toISOString().slice(0, 10);
}

export function computeStreak(results, today = todayParis()) {
  const won = new Set(results.filter((r) => r.won).map((r) => r.date));
  let cursor = won.has(today) ? today : addDaysStr(today, -1);
  let streak = 0;
  while (won.has(cursor)) {
    streak++;
    cursor = addDaysStr(cursor, -1);
  }
  return streak;
}

export function buildShareGrid(dayNumber, guesses, correctTrackId, won) {
  const squares = guesses
    .map((g) => (g === correctTrackId ? "🟩" : "🟥"))
    .join("");
  const result = won
    ? `${guesses.length}/${MAX_ATTEMPTS}`
    : `X/${MAX_ATTEMPTS}`;
  return `Zikle #${dayNumber} 🎧 ${result}\n\n${squares}\n\nhttps://www.zik-music.fr/zikle`;
}

export function escapeIlike(q) {
  return q.replace(/[%_]/g, (c) => `\\${c}`);
}

export function dedupById(rows) {
  const seen = new Set();
  const out = [];
  for (const row of rows) {
    if (seen.has(row.id)) continue;
    seen.add(row.id);
    out.push(row);
  }
  return out;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run src/lib/zikle/__tests__/shared.test.js`
Expected: PASS (10 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/zikle/shared.js src/lib/zikle/__tests__/shared.test.js
git commit -m "feat(zikle): module pur partagé (paliers, streak, partage, helpers)"
```

---

## Task 3: Service serveur — pool Deezer + pick quotidien (`src/lib/server/services/zikle.js`)

**Files:**

- Create: `src/lib/server/services/zikle.js`
- Test: `src/lib/server/services/__tests__/zikle.test.js`

**Interfaces:**

- Consumes: `getFetch` (`src/lib/server/services/fetch.js`), `parseExpFromUrl` (`src/lib/server/services/deezer.js`).
- Produces (utilisé par tasks 6, 7, 9, 10) : `mapDeezerChartTracks(json: object): {artist,title,preview_url,preview_expires_at,cover_url,external_id,source}[]`, `async refreshZiklePool(sb): Promise<{refreshed: boolean, added: number}>`, `async getOrCreateDailySong(sb, date?: string): Promise<{date: string, trackId: string} | null>`, `async getDayNumber(sb, date: string): Promise<number>`.

- [ ] **Step 1: Vérifier manuellement le format du chart Deezer**

Run: `curl -s "https://api.deezer.com/chart/0/tracks?limit=5"`
Vérifier que la réponse contient bien `data[].preview`, `data[].artist.name`, `data[].title`, `data[].album.cover_xl`, `data[].id`. Si `chart/0` ne reflète pas des titres FR mainstream reconnaissables, essayer `https://api.deezer.com/chart/0/tracks?limit=5&index=0` ou chercher l'id d'un chart éditorial France via `https://api.deezer.com/editorial` (à ajuster dans le Step 3 si besoin — le principe du code ne change pas, seule l'URL change).

- [ ] **Step 2: Write the failing test (partie pure)**

```javascript
// src/lib/server/services/__tests__/zikle.test.js
import { describe, it, expect, vi } from "vitest";
import {
  mapDeezerChartTracks,
  getDayNumber,
  getOrCreateDailySong,
} from "../zikle.js";

describe("mapDeezerChartTracks", () => {
  it("filtre les titres sans preview et mappe les champs Deezer", () => {
    const json = {
      data: [
        {
          id: 111,
          title: "Titre A",
          artist: { name: "Artiste A" },
          preview: "https://cdn.deezer.com/a.mp3",
          album: { cover_xl: "https://cdn.deezer.com/a.jpg" },
        },
        {
          id: 222,
          title: "Sans preview",
          artist: { name: "Artiste B" },
          preview: null,
          album: {},
        },
      ],
    };
    const rows = mapDeezerChartTracks(json);
    expect(rows).toHaveLength(1);
    expect(rows[0]).toMatchObject({
      artist: "Artiste A",
      title: "Titre A",
      preview_url: "https://cdn.deezer.com/a.mp3",
      cover_url: "https://cdn.deezer.com/a.jpg",
      external_id: "111",
      source: "deezer",
    });
  });
  it("tableau vide si data absent", () => {
    expect(mapDeezerChartTracks({})).toEqual([]);
  });
});

function fakeSb(overrides = {}) {
  return {
    from: vi.fn(() => ({
      select: vi.fn().mockReturnThis(),
      lte: vi.fn().mockReturnThis(),
      order: vi.fn().mockReturnThis(),
      limit: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      maybeSingle: vi.fn().mockResolvedValue({ data: null, error: null }),
      ...overrides,
    })),
    rpc: vi.fn(),
  };
}

describe("getDayNumber", () => {
  it("renvoie le count exact", async () => {
    const sb = {
      from: vi.fn(() => ({
        select: vi.fn(() => ({
          lte: vi.fn().mockResolvedValue({ count: 47, error: null }),
        })),
      })),
    };
    expect(await getDayNumber(sb, "2026-08-04")).toBe(47);
  });
});

describe("getOrCreateDailySong", () => {
  it("renvoie null si le pool est épuisé (RPC ne renvoie aucune ligne)", async () => {
    const sb = { rpc: vi.fn().mockResolvedValue({ data: [], error: null }) };
    expect(await getOrCreateDailySong(sb)).toBeNull();
  });
  it("renvoie date/trackId depuis la RPC", async () => {
    const sb = {
      rpc: vi.fn().mockResolvedValue({
        data: [{ date: "2026-08-04", track_id: "abc-123", is_new: true }],
        error: null,
      }),
    };
    expect(await getOrCreateDailySong(sb)).toEqual({
      date: "2026-08-04",
      trackId: "abc-123",
    });
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npx vitest run src/lib/server/services/__tests__/zikle.test.js`
Expected: FAIL (module `../zikle.js` introuvable).

- [ ] **Step 4: Write the implementation**

```javascript
// src/lib/server/services/zikle.js
import { getFetch } from "./fetch.js";
import { parseExpFromUrl } from "./deezer.js";

const REFRESH_INTERVAL_MS = 7 * 24 * 60 * 60 * 1000;
const DEEZER_CHART_URL = "https://api.deezer.com/chart/0/tracks?limit=100";
const PERMANENT_EXPIRY = "2099-01-01T00:00:00.000Z";

export function mapDeezerChartTracks(json) {
  const list = json?.data || [];
  return list
    .filter((t) => t?.preview && t?.artist?.name && t?.title)
    .map((t) => {
      const exp = parseExpFromUrl(t.preview);
      return {
        artist: t.artist.name,
        title: t.title,
        preview_url: t.preview,
        preview_expires_at: exp
          ? new Date(exp * 1000).toISOString()
          : PERMANENT_EXPIRY,
        cover_url: t.album?.cover_xl || t.album?.cover_big || null,
        external_id: String(t.id),
        source: "deezer",
      };
    });
}

export async function refreshZiklePool(sb) {
  const { data: latest } = await sb
    .from("zikle_pool")
    .select("added_at")
    .order("added_at", { ascending: false })
    .limit(1)
    .maybeSingle();

  if (
    latest &&
    Date.now() - new Date(latest.added_at).getTime() < REFRESH_INTERVAL_MS
  ) {
    return { refreshed: false, added: 0 };
  }

  const fetchFn = await getFetch();
  const res = await fetchFn(DEEZER_CHART_URL, {
    headers: { "User-Agent": "ZIK-BlindTest/1.0" },
    signal: AbortSignal.timeout(8000),
  });
  if (!res.ok) throw new Error(`HTTP ${res.status}`);
  const json = await res.json();
  const candidates = mapDeezerChartTracks(json);
  if (!candidates.length) return { refreshed: false, added: 0 };

  const { data: ids, error } = await sb.rpc("resolve_tracks", {
    p_tracks: candidates,
  });
  if (error) throw error;

  const rows = (ids || []).map((track_id) => ({ track_id }));
  if (!rows.length) return { refreshed: false, added: 0 };

  const { error: insErr } = await sb
    .from("zikle_pool")
    .upsert(rows, { onConflict: "track_id", ignoreDuplicates: true });
  if (insErr) throw insErr;

  return { refreshed: true, added: rows.length };
}

export async function getOrCreateDailySong(sb) {
  const { data, error } = await sb.rpc("pick_daily_song");
  if (error) throw error;
  const row = data?.[0];
  if (!row?.track_id) return null;
  return { date: row.date, trackId: row.track_id };
}

export async function getDayNumber(sb, date) {
  const { count, error } = await sb
    .from("daily_songs")
    .select("id", { count: "exact", head: true })
    .lte("date", date);
  if (error) throw error;
  return count;
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run src/lib/server/services/__tests__/zikle.test.js`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/lib/server/services/zikle.js src/lib/server/services/__tests__/zikle.test.js
git commit -m "feat(zikle): service pool Deezer + pick quotidien"
```

---

## Task 4: Endpoint autocomplete (`/api/tracks/search`)

**Files:**

- Create: `src/routes/(site)/api/tracks/search/+server.js`

**Interfaces:**

- Consumes: `escapeIlike`, `dedupById` (Task 2, `src/lib/zikle/shared.js`), `getAdminClient` (`src/lib/server/config.js`), `checkRateLimit` (`src/lib/server/middleware/auth.js`).
- Produces (utilisé par Task 8) : `GET /api/tracks/search?q=...` → `200 [{id, artist, title, cover_url}]` (max 8).

- [ ] **Step 1: Write the implementation**

```javascript
// src/routes/(site)/api/tracks/search/+server.js
import { json } from "@sveltejs/kit";
import { getAdminClient } from "$lib/server/config.js";
import { checkRateLimit } from "$lib/server/middleware/auth.js";
import { escapeIlike, dedupById } from "$lib/zikle/shared.js";

export async function GET({ request, url }) {
  const ip = request.headers.get("x-forwarded-for") || "unknown";
  checkRateLimit(ip, 30, 60_000);

  const q = url.searchParams.get("q")?.trim();
  if (!q || q.length < 2) return json([]);

  const sb = getAdminClient();
  const pattern = `%${escapeIlike(q)}%`;
  const [byArtist, byTitle] = await Promise.all([
    sb
      .from("tracks")
      .select("id, artist, title, cover_url")
      .ilike("artist", pattern)
      .limit(8),
    sb
      .from("tracks")
      .select("id, artist, title, cover_url")
      .ilike("title", pattern)
      .limit(8),
  ]);
  if (byArtist.error)
    return json({ error: byArtist.error.message }, { status: 400 });
  if (byTitle.error)
    return json({ error: byTitle.error.message }, { status: 400 });

  const merged = dedupById([
    ...(byArtist.data || []),
    ...(byTitle.data || []),
  ]).slice(0, 8);
  return json(merged);
}
```

- [ ] **Step 2: Test manuel**

Run: `npm run dev` puis dans un autre terminal `curl "http://localhost:5173/api/tracks/search?q=daft"`
Expected: tableau JSON de titres correspondants (peut être vide si le catalogue local n'a rien en base pour ce test — vérifier avec un artiste présent dans la BDD ZIK, ex: `curl "http://localhost:5173/api/tracks/search?q=a"` doit renvoyer jusqu'à 8 résultats).

- [ ] **Step 3: Commit**

```bash
git add "src/routes/(site)/api/tracks/search/+server.js"
git commit -m "feat(zikle): endpoint autocomplete /api/tracks/search"
```

---

## Task 5: Endpoint vérification (`/api/zikle/guess`)

**Files:**

- Create: `src/routes/(site)/api/zikle/guess/+server.js`

**Interfaces:**

- Consumes: `todayParis` (Task 2), `getAdminClient`, `verifyToken` (`src/lib/server/middleware/auth.js`).
- Produces (utilisé par Task 8) : `POST /api/zikle/guess` body `{date, track_id}` → `200 {correct: boolean}` | `400` | `401` | `404`.

- [ ] **Step 1: Write the implementation**

```javascript
// src/routes/(site)/api/zikle/guess/+server.js
import { json } from "@sveltejs/kit";
import { getAdminClient } from "$lib/server/config.js";
import { verifyToken } from "$lib/server/middleware/auth.js";
import { todayParis } from "$lib/zikle/shared.js";

const DATE_RE = /^\d{4}-\d{2}-\d{2}$/;

export async function POST({ request }) {
  let body;
  try {
    body = await request.json();
  } catch {
    return json({ error: "JSON invalide" }, { status: 400 });
  }

  const date = body?.date;
  const trackId = body?.track_id;
  if (!DATE_RE.test(date) || !trackId)
    return json({ error: "date et track_id requis" }, { status: 400 });

  const today = todayParis();
  if (date > today) return json({ error: "Date invalide" }, { status: 400 });

  if (date !== today) {
    const token = request.headers.get("authorization")?.slice(7);
    const user = token ? await verifyToken(token) : null;
    if (!user)
      return json(
        { error: "Connexion requise pour les archives" },
        { status: 401 },
      );
  }

  const sb = getAdminClient();
  const { data, error } = await sb
    .from("daily_songs")
    .select("track_id")
    .eq("date", date)
    .single();
  if (error || !data)
    return json({ error: "Aucune chanson pour cette date" }, { status: 404 });

  return json({ correct: data.track_id === trackId });
}
```

- [ ] **Step 2: Test manuel**

Run `npm run dev`, ouvrir `/zikle` pour déclencher le pick du jour (Task 7 doit être fait pour que la page existe — sinon tester directement via SQL : insérer une ligne test dans `daily_songs` avec `execute_sql`), puis :
`curl -X POST http://localhost:5173/api/zikle/guess -H "Content-Type: application/json" -d "{\"date\":\"<date_du_jour>\",\"track_id\":\"<un_uuid_quelconque>\"}"`
Expected: `{"correct":false}` (sauf coup de chance).

- [ ] **Step 3: Commit**

```bash
git add "src/routes/(site)/api/zikle/guess/+server.js"
git commit -m "feat(zikle): endpoint vérification /api/zikle/guess"
```

---

## Task 6: Endpoints fin de manche (`/api/zikle/complete` + `/api/zikle/leaderboard`)

**Files:**

- Create: `src/routes/(site)/api/zikle/complete/+server.js`
- Create: `src/routes/(site)/api/zikle/leaderboard/+server.js`

**Interfaces:**

- Consumes: `getAdminClient`, `verifyToken`.
- Produces (utilisé par Task 8 et Task 10) : `POST /api/zikle/complete` body `{date, attempts, won, solve_time_seconds, guesses}` → `200 {track: {artist,title,cover_url}, saved: {attempts,won,solve_time_seconds,is_new} | null}` ; `GET /api/zikle/leaderboard?date=YYYY-MM-DD` → `200 [{username, attempts, solve_time_seconds}]`.

- [ ] **Step 1: Write the implementation**

```javascript
// src/routes/(site)/api/zikle/complete/+server.js
import { json } from "@sveltejs/kit";
import { getAdminClient } from "$lib/server/config.js";
import { verifyToken } from "$lib/server/middleware/auth.js";

const DATE_RE = /^\d{4}-\d{2}-\d{2}$/;

export async function POST({ request }) {
  let body;
  try {
    body = await request.json();
  } catch {
    return json({ error: "JSON invalide" }, { status: 400 });
  }

  const { date, attempts, won, solve_time_seconds, guesses } = body || {};
  if (
    !DATE_RE.test(date) ||
    !Number.isInteger(attempts) ||
    attempts < 1 ||
    attempts > 6 ||
    typeof won !== "boolean" ||
    !Array.isArray(guesses)
  )
    return json({ error: "Payload invalide" }, { status: 400 });

  const sb = getAdminClient();
  const { data: songRow, error: songErr } = await sb
    .from("daily_songs")
    .select("tracks(artist, title, cover_url)")
    .eq("date", date)
    .single();
  if (songErr || !songRow)
    return json({ error: "Aucune chanson pour cette date" }, { status: 404 });

  const token = request.headers.get("authorization")?.slice(7);
  const user = token ? await verifyToken(token) : null;

  let saved = null;
  if (user) {
    const { data, error } = await sb.rpc("zikle_complete", {
      p_user_id: user.id,
      p_date: date,
      p_attempts: attempts,
      p_won: won,
      p_solve_time_seconds: Number.isInteger(solve_time_seconds)
        ? solve_time_seconds
        : null,
      p_guesses: guesses,
    });
    if (error) return json({ error: error.message }, { status: 400 });
    saved = data?.[0] ?? null;
  }

  return json({ track: songRow.tracks, saved });
}
```

- [ ] **Step 2: Write the leaderboard endpoint**

```javascript
// src/routes/(site)/api/zikle/leaderboard/+server.js
import { json } from "@sveltejs/kit";
import { getAdminClient } from "$lib/server/config.js";

const DATE_RE = /^\d{4}-\d{2}-\d{2}$/;

export async function GET({ url }) {
  const date = url.searchParams.get("date");
  if (!DATE_RE.test(date))
    return json({ error: "date requise (YYYY-MM-DD)" }, { status: 400 });

  const sb = getAdminClient();
  const { data, error } = await sb.rpc("zikle_leaderboard", { p_date: date });
  if (error) return json({ error: error.message }, { status: 400 });
  return json(data || []);
}
```

- [ ] **Step 3: Test manuel**

`curl -X POST http://localhost:5173/api/zikle/complete -H "Content-Type: application/json" -d "{\"date\":\"<date_du_jour>\",\"attempts\":3,\"won\":true,\"solve_time_seconds\":42,\"guesses\":[]}"`
Expected: `200` avec `{"track":{"artist":"...","title":"...","cover_url":"..."},"saved":null}` (saved null car pas de token = invité).

`curl "http://localhost:5173/api/zikle/leaderboard?date=<date_du_jour>"`
Expected: `200` avec un tableau (vide si personne de connecté n'a encore gagné aujourd'hui).

- [ ] **Step 4: Commit**

```bash
git add "src/routes/(site)/api/zikle/complete/+server.js" "src/routes/(site)/api/zikle/leaderboard/+server.js"
git commit -m "feat(zikle): endpoints fin de manche + classement du jour"
```

---

## Task 7: Page du jour — `/zikle/+page.server.js`

**Files:**

- Create: `src/routes/(site)/zikle/+page.server.js`

**Interfaces:**

- Consumes: `refreshZiklePool`, `getOrCreateDailySong`, `getDayNumber` (Task 3), `getAdminClient`.
- Produces (utilisé par Task 8) : `load()` → `{ date, dayNumber, previewUrl } | { date: null }` (pool épuisé/non alimenté).

- [ ] **Step 1: Write the implementation**

```javascript
// src/routes/(site)/zikle/+page.server.js
import { getAdminClient } from "$lib/server/config.js";
import {
  refreshZiklePool,
  getOrCreateDailySong,
  getDayNumber,
} from "$lib/server/services/zikle.js";

export async function load() {
  const sb = getAdminClient();
  await refreshZiklePool(sb);
  const daily = await getOrCreateDailySong(sb);
  if (!daily) return { date: null, dayNumber: null, previewUrl: null };

  const [{ data: track }, dayNumber] = await Promise.all([
    sb.from("tracks").select("preview_url").eq("id", daily.trackId).single(),
    getDayNumber(sb, daily.date),
  ]);

  return {
    date: daily.date,
    dayNumber,
    previewUrl: track?.preview_url ?? null,
  };
}
```

- [ ] **Step 2: Test manuel**

Run `npm run dev`, naviguer sur `http://localhost:5173/zikle` (la page `+page.svelte` n'existe pas encore, SvelteKit affichera une erreur 404 de rendu — c'est attendu à ce stade). Vérifier plutôt via les logs serveur / `execute_sql` que `daily_songs` contient bien une nouvelle ligne pour aujourd'hui après une requête (ajouter temporairement un `console.log(daily)` si besoin, à retirer avant commit).

- [ ] **Step 3: Commit**

```bash
git add "src/routes/(site)/zikle/+page.server.js"
git commit -m "feat(zikle): load /zikle (refresh pool + pick du jour)"
```

---

## Task 8: Composant de jeu partagé + page du jour (`ZikleGame.svelte` + `/zikle/+page.svelte`)

**Files:**

- Create: `src/lib/components/ZikleGame.svelte`
- Create: `src/routes/(site)/zikle/+page.svelte`

**Interfaces:**

- Consumes: `data` de Task 7 (`date`, `dayNumber`, `previewUrl`), `getContext('zik')` (`sb`, `user` — pattern `profile/+page.svelte`), `durationForAttempt`, `MAX_ATTEMPTS`, `computeStreak`, `buildShareGrid` (Task 2), endpoints `/api/tracks/search`, `/api/zikle/guess`, `/api/zikle/complete`, `/api/zikle/leaderboard` (Tasks 4-6).
- Produces (utilisé par Task 10) : `ZikleGame.svelte` avec props `date: string`, `dayNumber: number`, `previewUrl: string | null` — encapsule toute la mécanique de jeu (audio, recherche, essais, révélation, streak, classement, partage), y compris la branche invité (`localStorage`) vs connecté (`daily_results` via `sb`). Ce composant ne gère pas l'authentification (c'est au parent de décider quand le monter) et fonctionne à l'identique que le parent soit `/zikle` (pas de mur) ou `/zikle/archives/[date]` (mur d'auth, cf. Task 10).

- [ ] **Step 1: Write `ZikleGame.svelte`**

Interface minimale, fonctionnelle (pas de style poussé — à itérer plus tard) : lecteur audio coupé au palier courant, champ de recherche avec autocomplete, liste des essais, révélation + partage + classement en fin de manche. Gère lui-même l'état "déjà joué" (localStorage pour l'invité, sinon rejoué normalement à chaque montage — l'unicité serveur de `daily_results` empêche de toute façon de re-sauver un résultat déjà connu pour un connecté).

```svelte
<script>
  import { getContext, onMount } from "svelte";
  import {
    MAX_ATTEMPTS,
    durationForAttempt,
    computeStreak,
    buildShareGrid,
  } from "$lib/zikle/shared.js";

  let { date, dayNumber, previewUrl } = $props();

  const _ctx = getContext("zik");
  const sb = _ctx.sb;
  const user = $derived(_ctx.user);

  const STORAGE_KEY = "zikle_history"; // { [date]: { attempts: string[], won: boolean } }

  let audioEl;
  let query = $state("");
  let suggestions = $state([]);
  let attempts = $state([]); // track_id[] tentés
  let attemptLabels = $state([]); // "Artiste - Titre" affichés dans la grille des essais
  let finished = $state(false);
  let won = $state(false);
  let reveal = $state(null); // { artist, title, cover_url }
  let streak = $state(0);
  let startedAt = $state(0);
  let shareCopied = $state(false);
  let leaderboard = $state([]);

  function loadGuestHistory() {
    try {
      return JSON.parse(localStorage.getItem(STORAGE_KEY) || "{}");
    } catch {
      return {};
    }
  }

  function saveGuestResult(d, attemptIds, isWon) {
    const history = loadGuestHistory();
    history[d] = { attempts: attemptIds, won: isWon };
    localStorage.setItem(STORAGE_KEY, JSON.stringify(history));
  }

  async function computeAndSetStreak() {
    if (user) {
      const { data: rows } = await sb
        .from("daily_results")
        .select("date, won")
        .order("date", { ascending: false })
        .limit(400);
      streak = computeStreak(rows || [], date);
    } else {
      const history = loadGuestHistory();
      const rows = Object.entries(history).map(([d, v]) => ({ date: d, won: v.won }));
      streak = computeStreak(rows, date);
    }
  }

  onMount(() => {
    startedAt = Date.now();
    const history = loadGuestHistory();
    const todays = history[date];
    if (todays && !user) {
      finished = true;
      won = todays.won;
      attempts = todays.attempts;
    }
    computeAndSetStreak();
  });

  function playSnippet() {
    if (!audioEl) return;
    const dur = durationForAttempt(attempts.length);
    audioEl.currentTime = 0;
    audioEl.play();
    const stop = () => {
      if (audioEl.currentTime >= dur) {
        audioEl.pause();
        audioEl.removeEventListener("timeupdate", stop);
      }
    };
    audioEl.addEventListener("timeupdate", stop);
  }

  let searchTimer;
  function onQueryInput() {
    clearTimeout(searchTimer);
    if (query.trim().length < 2) {
      suggestions = [];
      return;
    }
    searchTimer = setTimeout(async () => {
      const res = await fetch(`/api/tracks/search?q=${encodeURIComponent(query.trim())}`);
      suggestions = res.ok ? await res.json() : [];
    }, 200);
  }

  async function authHeader() {
    if (!user) return {};
    const { data: session } = await sb.auth.getSession();
    const token = session?.session?.access_token;
    return token ? { Authorization: `Bearer ${token}` } : {};
  }

  async function submitGuess(track) {
    if (finished) return;
    const res = await fetch("/api/zikle/guess", {
      method: "POST",
      headers: { "Content-Type": "application/json", ...(await authHeader()) },
      body: JSON.stringify({ date, track_id: track.id }),
    });
    const { correct } = await res.json();
    attempts = [...attempts, track.id];
    attemptLabels = [...attemptLabels, `${track.artist} - ${track.title}`];
    query = "";
    suggestions = [];

    if (correct) {
      await endRound(true);
    } else if (attempts.length >= MAX_ATTEMPTS) {
      await endRound(false);
    }
  }

  async function skipAttempt() {
    if (finished) return;
    attempts = [...attempts, "__skip__"];
    attemptLabels = [...attemptLabels, "(passé)"];
    if (attempts.length >= MAX_ATTEMPTS) await endRound(false);
  }

  async function endRound(isWon) {
    finished = true;
    won = isWon;
    const solveTime = Math.round((Date.now() - startedAt) / 1000);

    const res = await fetch("/api/zikle/complete", {
      method: "POST",
      headers: { "Content-Type": "application/json", ...(await authHeader()) },
      body: JSON.stringify({
        date,
        attempts: attempts.length,
        won: isWon,
        solve_time_seconds: solveTime,
        guesses: attempts,
      }),
    });
    const payload = await res.json();
    reveal = payload.track;

    const lbRes = await fetch(`/api/zikle/leaderboard?date=${date}`);
    leaderboard = lbRes.ok ? await lbRes.json() : [];

    if (!user) saveGuestResult(date, attempts, isWon);
    await computeAndSetStreak();
  }

  async function share() {
    const text = buildShareGrid(dayNumber, attempts, won ? attempts.at(-1) : "", won);
    if (navigator.share) {
      await navigator.share({ text });
    } else {
      await navigator.clipboard.writeText(text);
      shareCopied = true;
      setTimeout(() => (shareCopied = false), 2000);
    }
  }
</script>

<audio bind:this={audioEl} src={previewUrl} preload="auto"></audio>

{#if streak > 0}
  <p class="zikle-streak">🔥 Série : {streak} jour{streak > 1 ? "s" : ""}</p>
{/if}

{#if !finished}
  <button onclick={playSnippet}>▶️ Écouter ({durationForAttempt(attempts.length)}s)</button>

  <div class="zikle-search">
    <input
      type="text"
      placeholder="Titre ou artiste..."
      bind:value={query}
      oninput={onQueryInput}
    />
    {#if suggestions.length}
      <ul class="zikle-suggestions">
        {#each suggestions as s (s.id)}
          <li>
            <button onclick={() => submitGuess(s)}>{s.artist} - {s.title}</button>
          </li>
        {/each}
      </ul>
    {/if}
  </div>

  <button onclick={skipAttempt}>Passer</button>

  <ol class="zikle-attempts">
    {#each attemptLabels as label}
      <li>❌ {label}</li>
    {/each}
  </ol>
  <p>{attempts.length} / {MAX_ATTEMPTS} essais</p>
{:else}
  <div class="zikle-result">
    {#if won}
      <p>🎉 Trouvé en {attempts.length} essai{attempts.length > 1 ? "s" : ""} !</p>
    {:else}
      <p>😢 Perdu — c'était :</p>
    {/if}
    {#if reveal}
      <p><strong>{reveal.artist} - {reveal.title}</strong></p>
      {#if reveal.cover_url}<img src={reveal.cover_url} alt="" width="120" />{/if}
    {/if}
    <button onclick={share}>Partager le résultat</button>
    {#if shareCopied}<span>Copié !</span>{/if}
    {#if leaderboard.length}
      <h2>Classement du jour</h2>
      <ol>
        {#each leaderboard as row}
          <li>{row.username} — {row.attempts} essai{row.attempts > 1 ? "s" : ""}{row.solve_time_seconds ? ` (${row.solve_time_seconds}s)` : ""}</li>
        {/each}
      </ol>
    {/if}
  </div>
{/if}
```

- [ ] **Step 2: Write `/zikle/+page.svelte`**

```svelte
<script>
  import ZikleGame from "$lib/components/ZikleGame.svelte";

  let { data } = $props();
</script>

<svelte:head>
  <title>Zikle #{data.dayNumber ?? ""} — Devine la chanson du jour | ZIK</title>
  <meta name="description" content="Zikle : devine la chanson du jour à partir d'un extrait audio qui s'allonge à chaque essai. 6 essais, un nouveau titre chaque jour, gratuit et sans compte." />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href="https://www.zik-music.fr/zikle" />
  <meta property="og:title" content="Zikle — Devine la chanson du jour" />
  <meta property="og:description" content="Un extrait audio qui s'allonge à chaque essai raté. 6 essais pour deviner le titre du jour." />
  <meta property="og:url" content="https://www.zik-music.fr/zikle" />
  <meta property="og:type" content="website" />
  <meta property="og:image" content="https://www.zik-music.fr/og.png" />
</svelte:head>

<div class="zikle-page">
  <h1>Zikle {data.dayNumber ? `#${data.dayNumber}` : ""}</h1>

  {#if !data.date}
    <p>Pas de chanson disponible aujourd'hui, reviens un peu plus tard 🙏</p>
  {:else}
    <ZikleGame date={data.date} dayNumber={data.dayNumber} previewUrl={data.previewUrl} />
  {/if}
</div>
```

- [ ] **Step 3: Test manuel dans le navigateur**

Run `npm run dev`, ouvrir `http://localhost:5173/zikle` :

- Vérifier que le titre "Zikle #N" s'affiche et que le bouton "Écouter" joue bien un extrait de ~1s puis se coupe.
- Taper 2+ caractères dans la recherche → suggestions apparaissent.
- Faire 6 essais ratés volontairement → écran de révélation + bouton partage fonctionnel (clipboard ou share natif).
- Recharger la page (invité) → la partie du jour doit rester marquée comme terminée (lue depuis `localStorage`).

- [ ] **Step 4: Commit**

```bash
git add src/lib/components/ZikleGame.svelte "src/routes/(site)/zikle/+page.svelte"
git commit -m "feat(zikle): composant de jeu partagé + interface du jour"
```

---

## Task 9: Archives — liste (`/zikle/archives`)

**Files:**

- Create: `src/routes/(site)/zikle/archives/+page.server.js`
- Create: `src/routes/(site)/zikle/archives/+page.svelte`

**Interfaces:**

- Consumes: `getAdminClient`, `getDayNumber` (Task 3).
- Produces: `load()` → `{ days: {date: string, dayNumber: number}[] }` (ordre décroissant, exclut aujourd'hui).

- [ ] **Step 1: Write `+page.server.js`**

```javascript
// src/routes/(site)/zikle/archives/+page.server.js
import { getAdminClient } from "$lib/server/config.js";
import { todayParis } from "$lib/zikle/shared.js";

export async function load() {
  const sb = getAdminClient();
  const today = todayParis();
  const { data, error } = await sb
    .from("daily_songs")
    .select("date")
    .lt("date", today)
    .order("date", { ascending: false })
    .limit(200);
  if (error) return { days: [] };

  const total = data.length;
  const days = data.map((row, i) => ({ date: row.date, dayNumber: total - i }));
  return { days };
}
```

- [ ] **Step 2: Write `+page.svelte`**

```svelte
<script>
  let { data } = $props();
</script>

<svelte:head>
  <title>Zikle — Archives | ZIK</title>
  <meta name="description" content="Rejoue les Zikle des jours précédents (compte requis)." />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href="https://www.zik-music.fr/zikle/archives" />
</svelte:head>

<div class="zikle-archives">
  <h1>Archives Zikle</h1>
  {#if !data.days.length}
    <p>Pas encore d'archives, reviens demain !</p>
  {:else}
    <ul>
      {#each data.days as day (day.date)}
        <li><a href="/zikle/archives/{day.date}">Zikle #{day.dayNumber} — {day.date}</a></li>
      {/each}
    </ul>
  {/if}
</div>
```

- [ ] **Step 3: Test manuel**

Run `npm run dev`, ouvrir `/zikle/archives` : doit lister les jours précédents (vide le premier jour d'utilisation, c'est attendu).

- [ ] **Step 4: Commit**

```bash
git add "src/routes/(site)/zikle/archives/+page.server.js" "src/routes/(site)/zikle/archives/+page.svelte"
git commit -m "feat(zikle): page archives (liste des jours passés)"
```

---

## Task 10: Archives — rejouer un jour (`/zikle/archives/[date]`)

**Files:**

- Create: `src/routes/(site)/zikle/archives/[date]/+page.server.js`
- Create: `src/routes/(site)/zikle/archives/[date]/+page.svelte`

**Interfaces:**

- Consumes: `getAdminClient`, `getDayNumber` (Task 3), `todayParis` (Task 2), `ZikleGame.svelte` (Task 8) monté seulement si `user` est présent, mur d'authentification repris du pattern `profile/+page.svelte` (`{:else if !user}` → "Connexion requise").

- [ ] **Step 1: Write `+page.server.js`**

```javascript
// src/routes/(site)/zikle/archives/[date]/+page.server.js
import { error, redirect } from "@sveltejs/kit";
import { getAdminClient } from "$lib/server/config.js";
import { getDayNumber, todayParis } from "$lib/server/services/zikle.js";

const DATE_RE = /^\d{4}-\d{2}-\d{2}$/;

export async function load({ params }) {
  if (!DATE_RE.test(params.date)) throw error(400, "Date invalide");
  const today = todayParis();
  if (params.date >= today) throw redirect(302, "/zikle");

  const sb = getAdminClient();
  const { data: song, error: songErr } = await sb
    .from("daily_songs")
    .select("track_id, tracks(preview_url)")
    .eq("date", params.date)
    .single();
  if (songErr || !song) throw error(404, "Pas de Zikle pour cette date");

  const dayNumber = await getDayNumber(sb, params.date);
  return {
    date: params.date,
    dayNumber,
    previewUrl: song.tracks?.preview_url ?? null,
  };
}
```

Note : `todayParis` est ré-exportée depuis `src/lib/zikle/shared.js` — l'ajouter aux exports consommés par `src/lib/server/services/zikle.js` en l'important et la ré-exportant (`export { todayParis } from "$lib/zikle/shared.js";` en tête du fichier de la Task 3), pour éviter d'importer deux modules différents dans ce fichier.

- [ ] **Step 2: Write `+page.svelte`**

Réutilise `ZikleGame` (Task 8) — pas de duplication de la mécanique de jeu. Seule différence : mur d'authentification avant de monter le composant.

```svelte
<script>
  import { getContext } from "svelte";
  import ZikleGame from "$lib/components/ZikleGame.svelte";

  let { data } = $props();

  const _ctx = getContext("zik");
  const user = $derived(_ctx.user);
  const authReady = $derived(_ctx.authReady);
  const openAuthModal = _ctx.openAuthModal;
</script>

<svelte:head>
  <title>Zikle #{data.dayNumber} — Archives | ZIK</title>
  <meta name="description" content="Rejoue le Zikle #{data.dayNumber} ({data.date}). Compte requis pour jouer aux archives." />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href="https://www.zik-music.fr/zikle/archives/{data.date}" />
</svelte:head>

<div class="zikle-page">
  <h1>Zikle #{data.dayNumber} — {data.date}</h1>

  {#if !authReady}
    <p>Chargement...</p>
  {:else if !user}
    <div class="zikle-auth-wall">
      <p>🔒 Connecte-toi pour rejouer les archives Zikle.</p>
      <button onclick={() => openAuthModal("login")}>Se connecter</button>
    </div>
  {:else}
    <ZikleGame date={data.date} dayNumber={data.dayNumber} previewUrl={data.previewUrl} />
  {/if}
</div>
```

- [ ] **Step 3: Ajouter le ré-export `todayParis` dans le service (Task 3)**

Ouvrir `src/lib/server/services/zikle.js` et ajouter en tête de fichier :

```javascript
export { todayParis } from "$lib/zikle/shared.js";
```

- [ ] **Step 4: Test manuel**

Sans compte : ouvrir `/zikle/archives/<une_date_passée>` → doit afficher le mur "Connexion requise". Avec compte connecté : le jeu doit s'afficher et fonctionner comme sur `/zikle`.

- [ ] **Step 5: Commit**

```bash
git add "src/routes/(site)/zikle/archives/[date]" src/lib/server/services/zikle.js
git commit -m "feat(zikle): rejouer une archive (compte requis)"
```

---

## Task 11: Lien de navigation (footer)

**Files:**

- Modify: `src/routes/(site)/+layout.svelte` (section "Jouer" du footer, cf. lignes ~176-184 vues en exploration — utiliser Grep pour retrouver l'emplacement exact avant d'éditer, les numéros de ligne peuvent avoir bougé)

**Interfaces:** aucune (changement HTML pur).

- [ ] **Step 1: Ajouter le lien**

Dans le bloc `<div class="footer-col-links">` de la colonne "Jouer", ajouter une ligne :

```svelte
<a href="/zikle">Zikle</a>
```

juste avant ou après `<a href="/rooms">Rooms</a>` (ordre libre, cohérent avec le reste).

Note : l'intégration dans la nav principale (`src/lib/components/Nav.svelte`, bottom-nav mobile inclus) n'est volontairement pas traitée ici — l'interface Zikle doit encore être itérée (cf. échange de brainstorming), un lien footer suffit pour tester en local. À ajouter à la nav principale une fois l'UI stabilisée.

- [ ] **Step 2: Test manuel**

Vérifier dans le navigateur que le lien apparaît dans le footer et mène bien à `/zikle`.

- [ ] **Step 3: Commit**

```bash
git add "src/routes/(site)/+layout.svelte"
git commit -m "feat(zikle): lien footer vers /zikle"
```

---

## Task 12: Vérification finale en local

**Files:** aucun (validation manuelle).

- [ ] **Step 1: Lancer la suite de tests**

Run: `npm test`
Expected: tous les tests passent, y compris les nouveaux (`shared.test.js`, `zikle.test.js`).

- [ ] **Step 2: Lint**

Run: `npm run lint`
Expected: aucune erreur (règle CLAUDE.md : toujours lint avant PR).

- [ ] **Step 3: Parcours complet en navigateur**

Run `npm run dev`, puis dans le navigateur :

1. `/zikle` en étant déconnecté : jouer une manche complète (victoire ou défaite), vérifier le partage, recharger la page → état "déjà joué" conservé.
2. Se connecter, rejouer un autre jour n'est pas possible le même jour (uniqueness), vérifier en base (`execute_sql` sur `daily_results`) que la ligne a bien été créée et que `profiles.xp` a augmenté de 10 en cas de victoire. Vérifier aussi que le compte apparaît dans le "Classement du jour" affiché après une victoire.
3. `/zikle/archives` : la liste doit apparaître dès le 2e jour d'utilisation (vide au tout premier lancement — normal).
4. `/zikle/archives/<date>` déconnecté → mur de connexion ; connecté → jeu jouable.

- [ ] **Step 4: Commit final si des ajustements ont été faits pendant la vérification**

```bash
git add -A
git commit -m "fix(zikle): ajustements suite aux tests manuels"
```

(uniquement si des changements ont eu lieu — sinon, rien à committer, la vérification s'arrête au Step 3)
